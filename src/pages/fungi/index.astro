---
/**
 * 🍄 FUNGI INDEX PAGE
 * ===================
 * 
 * Main page showing all fungi with AMORPH morphs
 * Loads real data from Convex
 */

import BaseLayout from '@/amorph/arch/layouts/BaseLayout.astro';
import { fetchFungi } from '@/amorph/arch/convex';

// Fetch fungi data from Convex
const fungi = await fetchFungi();

console.log(`📊 Loaded ${fungi?.length || 0} fungi from Convex`);
---

<BaseLayout 
  title="Pilze - Funginomi AMORPH"
  description="Morphologische Pilz-Datenbank mit perspektivischen Ansichten"
  enableGlow={true}
  enableSearch={true}
  enableAnimation={true}
>
  <!-- Page Header -->
  <header style="margin-bottom: 2rem; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
    <div>
      <h1 style="font-size: 2.5rem; font-weight: 700; color: white; margin-bottom: 0.5rem;">
        🍄 Pilze Datenbank
      </h1>
      <p style="font-size: 1.125rem; color: rgba(255, 255, 255, 0.8);">
        {fungi?.length || 0} Pilze · Morphologische Ansichten · Perspektivisches Wissen
      </p>
    </div>
    
    <!-- View Toggle -->
    <div class="view-toggle">
      <button class="view-btn active" data-view="grid">
        📊 Grid View
      </button>
      <button class="view-btn" data-view="bubble">
        🫧 Bubble View
      </button>
    </div>
  </header>

  <!-- Bubble Host -->
  <bubble-host id="bubble-view-host" style="display: none;"></bubble-host>

  <!-- Fungi Grid -->
  <div class="fungi-grid" id="grid-view-host">
    {fungi && fungi.length > 0 ? (
      fungi.map((fungus: any) => (
        <div class="fungus-card">
          {fungus.imageUrl && (
            <div class="card-image">
              <image-morph 
                src={fungus.imageUrl} 
                alt={fungus.commonName}
              ></image-morph>
            </div>
          )}
          
          <div class="card-header">
            <name-morph 
              value={fungus.commonName} 
              size="large"
            ></name-morph>
            <name-morph 
              value={fungus.latinName} 
              perspective="scientific"
              lang="la"
              size="small"
            ></name-morph>
          </div>

          <div class="card-data">
            <data-morph
              fungus-data={JSON.stringify(fungus)}
              field="edibility"
            ></data-morph>

            <data-morph
              fungus-data={JSON.stringify(fungus)}
              field="habitat"
            ></data-morph>

            <data-morph
              fungus-data={JSON.stringify(fungus)}
              field="sporePrintColor"
            ></data-morph>
          </div>

          <a href={`/fungi/${fungus.slug}`} class="card-link">
            Details ansehen →
          </a>
        </div>
      ))
    ) : (
      <div class="empty-state">
        <p style="font-size: 1.5rem; color: rgba(255, 255, 255, 0.7); text-align: center;">
          🍄 Keine Pilze gefunden
        </p>
        <p style="color: rgba(255, 255, 255, 0.5); text-align: center; margin-top: 0.5rem;">
          Stelle sicher, dass Convex Daten enthält und PUBLIC_CONVEX_URL korrekt ist.
        </p>
      </div>
    )}
  </div>
</BaseLayout>

<style>
  .fungus-card {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .fungus-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.3);
    border-color: rgba(255, 255, 255, 0.2);
  }

  .card-image {
    width: 100%;
    height: 200px;
    overflow: hidden;
    background: rgba(0, 0, 0, 0.3);
  }

  .card-header {
    padding: 1.5rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.02);
  }

  .card-header name-morph {
    display: block;
  }

  .card-header name-morph:first-child {
    margin-bottom: 0.5rem;
  }

  .card-data {
    padding: 1.5rem;
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .card-data data-morph {
    margin-bottom: 0;
  }

  .card-link {
    display: block;
    padding: 1rem 1.5rem;
    text-align: center;
    background: rgba(255, 255, 255, 0.05);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    color: white;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.2s ease;
  }

  .card-link:hover {
    background: rgba(255, 255, 255, 0.1);
    color: #22c55e;
  }

  /* View Toggle */
  .view-toggle {
    display: flex;
    gap: 0.5rem;
    background: rgba(255, 255, 255, 0.1);
    padding: 0.25rem;
    border-radius: 12px;
    backdrop-filter: blur(10px);
  }

  .view-btn {
    padding: 0.75rem 1.5rem;
    background: transparent;
    border: none;
    border-radius: 8px;
    color: rgba(255, 255, 255, 0.7);
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .view-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    color: white;
  }

  .view-btn.active {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
  }

  /* Bubble Host */
  #bubble-view-host {
    margin-bottom: 2rem;
  }

  /* Fungi Grid */
  .fungi-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
  }
  
  /* Fungus Card */
  .fungus-card {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(20px);
    border-radius: 16px;
    padding: 1.5rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: all 0.3s ease;
  }
  
  .fungus-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.15);
  }
  
  /* Morphs */
  .morphs {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  
  /* Tags */
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin: 0.5rem 0;
  }
  
  /* Detail Link */
  .detail-link {
    display: inline-block;
    margin-top: 1rem;
    padding: 0.5rem 1rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 500;
    transition: all 0.2s ease;
  }
  
  .detail-link:hover {
    transform: translateX(4px);
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
  }
  
  /* Empty State */
  .empty-state {
    grid-column: 1 / -1;
    padding: 4rem 2rem;
    text-align: center;
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .fungi-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
    
    .fungus-card {
      padding: 1rem;
    }

    header {
      flex-direction: column;
      align-items: flex-start !important;
    }
  }
  
  /* Dark Mode */
  @media (prefers-color-scheme: dark) {
    .fungus-card {
      background: rgba(30, 30, 30, 0.9);
      color: rgba(255, 255, 255, 0.9);
    }
  }
</style>

<script define:vars={{ fungi }}>
  // Wait for AMORPH and hosts to load
  function initViewToggle() {
    const amorph = window.amorph;
    if (!amorph) {
      console.log('⏳ Waiting for AMORPH...');
      setTimeout(initViewToggle, 100);
      return;
    }

    // Get host elements
    const bubbleHost = document.getElementById('bubble-view-host');
    const gridHost = document.getElementById('grid-view-host');
    
    if (!bubbleHost || !gridHost) {
      console.warn('⚠️ Host elements not found, retrying...');
      setTimeout(initViewToggle, 100);
      return;
    }

    // Wait for bubbleHost to be fully upgraded (web component ready)
    if (!bubbleHost.setData) {
      console.log('⏳ Waiting for BubbleHost to upgrade...');
      setTimeout(initViewToggle, 100);
      return;
    }

    // DATENGETRIEBEN: Übergebe Fungi-Daten an BubbleHost
    console.log('📊 Fungi data:', fungi);
    bubbleHost.setData(fungi);
    console.log(`✅ Passed ${fungi.length} fungi to BubbleHost`);

    // Initialize with default perspectives for DataMorphs
    const defaultPerspectives = ['culinaryAndNutritional', 'ecologyAndHabitat'];
    window.dispatchEvent(new CustomEvent('perspective-changed', {
      detail: { perspectives: defaultPerspectives }
    }));
    console.log('✅ DataMorphs initialized with perspectives:', defaultPerspectives);

    // View Toggle functionality
    const viewBtns = document.querySelectorAll('.view-btn');

    viewBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        const view = btn.dataset.view;
        
        // Update active state
        viewBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        
        // Toggle views
        if (view === 'grid') {
          gridHost.style.display = 'grid';
          bubbleHost.style.display = 'none';
          console.log('📊 Switched to Grid View');
        } else {
          gridHost.style.display = 'none';
          bubbleHost.style.display = 'block';
          console.log('🫧 Switched to Bubble View');
        }
      });
    });

    console.log('✅ View toggle initialized with', fungi.length, 'fungi');
  }

  // Start initialization
  console.log('🚀 Starting view toggle initialization...');
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initViewToggle);
  } else {
    initViewToggle();
  }
</script>
